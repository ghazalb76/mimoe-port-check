---
description: Append a new dated session entry to NOTES.md
---
Append a new entry to the end of `NOTES.md`, following the exact structure
already used by the existing entries: a `## Session N -- <topic> --
<date>` heading, then what was verified before/during the session, what was
asked for, what was changed or rejected and why, and -- if applicable -- any
decision made autonomously mid-session that's being flagged for review
rather than presented as if it had been requested (see the Session 1
"Claude's call, not mine" entry for the pattern).

Base it on this conversation's actual work -- don't write a generic
summary. Number the session sequentially after the last one in the file.
Never include real system output (process names, ports, PIDs, paths) --
paraphrase or use placeholders, same as every existing entry.
